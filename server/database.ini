// These settings are for connecting to the ECMNet database.  At this time, the ECMNet
// server only supports connections through ODBC via driver (which does not require
// a DSN or entry in odbc.ini) or via a DSN.  If both are specified, then the ECMNet
// will connect via the DSN.

// driver=MySQL ODBC 5.1 Driver
// server=localhost
// port=3306
// database=ecmnet
// user=ecmnet
// password=Password_for_ecmnet_user

// dsn=ECMNet_PostgreSQL
// server=localhost
// port=5432
// database=ecmnet
// user=ecmnet
// password=Password_for_ecmnet_user
